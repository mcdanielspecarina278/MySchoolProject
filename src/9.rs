fn main() {
    // Initialize variables
    let mut x = 0;
    let y = 5;

    // Print a message to the console
    println!("Hello, world!");

    // Perform some calculations
    x += 1;
    x *= y;

    // Print the result
    println!("The result is: {}", x);
}
