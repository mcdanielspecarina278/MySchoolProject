use std::collections::HashMap;

fn main() {
    let mut grades = HashMap::new();
    grades.insert(String::from("Math"), 92);
    grades.insert(String::from("Science"), 85);
    grades.insert(String::from("History"), 76);
    grades.insert(String::from("English"), 90);
}
